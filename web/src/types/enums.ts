export enum FrequencyType {
  Daily = "DAILY",
  CustomDays = "CUSTOM_DAYS",
  XPerWeek = "X_PER_WEEK",
}

export enum ScheduleType {
  Once = "ONCE",
  Recurring = "RECURRING",
}

export enum GoalEntryStatus {
  Pending = "PENDING",
  Completed = "COMPLETED",
  Failed = "FAILED",
}
